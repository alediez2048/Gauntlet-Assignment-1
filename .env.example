# CollabBoard — copy to .env.local and fill in values. Never commit .env.local.

# Supabase (from project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# WebSocket Server (TICKET-03+)
# Local development:
NEXT_PUBLIC_WS_URL=ws://localhost:4000
# Production (Railway):
# NEXT_PUBLIC_WS_URL=wss://your-app.railway.app

# Optional: for server/ (TICKET-03). Create server/.env when you add the WS server.
# SUPABASE_JWT_SECRET=your-jwt-secret

# ── AI Agent (TICKET-11+) ────────────────────────────────────────────────────

# OpenAI — required for AI command route
# OPENAI_API_KEY=sk-...

# AI Bridge — shared secret between Next.js and the realtime server.
# Set the same value in both .env.local (Next.js) and server/.env (realtime server).
# Generate with: openssl rand -hex 32
# AI_BRIDGE_SECRET=your-bridge-secret

# Realtime server URL as seen from the Next.js process (server-side only, not NEXT_PUBLIC_)
# Local development:
# REALTIME_SERVER_URL=http://localhost:4000
# Production:
# REALTIME_SERVER_URL=https://your-app.railway.app

# ── Observability / Tracing (TICKET-11+ — from 1.3-langsmith-langfuse reference) ──

# Langfuse — sends AI traces to Langfuse cloud or self-hosted
# Get keys from https://cloud.langfuse.com → Settings → API Keys
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_HOST=https://us.cloud.langfuse.com

# LangSmith — auto-instruments OpenAI calls when LANGCHAIN_TRACING_V2=true (TICKET-12)
# Get key from https://smith.langchain.com → Settings → API Keys
# LANGCHAIN_API_KEY=lsv2_...
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_PROJECT=collabboard-ai
